#include <stdio.h>
#include <stdlib.h>

int main()
{ int n,i,p;
    printf("enter n:number");
    scanf("%d",&n);
    i=0;
    p=1;
    for(i=1;i<=n;i++)                           // 1+3+5+7
        i=pow(p,2)+2;
        printf("%d",p);

    return 0;
}
